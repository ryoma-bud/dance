spring.application.name=demo

# Tomcat Server Port Setting
server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5432/danstar
spring.datasource.username=postgres
spring.datasource.password=Hydralisk1!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# spring.mail.username=zergling2565@gmail.com
# spring.mail.password=wqtnzvrmakccxzsp

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# JWT
#jwt.secret=my-super-secret-key-for-danstar-application-change-this-later
jwt.secret.key=${JWT_SECRET_KEY}
# 86400000 = 24Hours
jwt.expiration=3600000
